kind: Service
apiVersion: v1
metadata:
  name: egrocer-customer
  labels:
    name: egrocer-customer
spec:
  type: LoadBalancer
  ports:
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:           
    app: egrocer-customer        

---
apiVersion: apps/v1 
kind: Deployment    
metadata:              
  name: egrocer-customer
spec:                
  selector:         
    matchLabels:
      app: egrocer-customer
  replicas: 3        
  template:
    metadata:
      labels:        
        app: egrocer-customer
    spec:
      containers:
        - name: egrocer-customer
          image: linjudock19/egrocer-customer
          ports:
            - containerPort: 8080                
          env:   # Setting Enviornmental Variables
          - name: DB_HOST   # Setting Database host address from configMap
            valueFrom: 
              configMapKeyRef:
                name: db-conf  # name of configMap
                key: host
          - name: DB_NAME  # Setting Database name from configMap
            valueFrom:
              configMapKeyRef:
                name: db-conf 
                key: name
          - name: DB_USERNAME  # Setting Database username from Secret
            valueFrom:
              secretKeyRef:
                name: db-credentials # Secret Name
                key: username
          - name: DB_PASSWORD # Setting Database password from Secret
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: password     
        